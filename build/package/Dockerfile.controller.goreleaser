FROM alpine:3.20.0
RUN apk add --no-cache ca-certificates && \
    addgroup -S draforge && adduser -S draforge -G draforge
USER draforge
WORKDIR /home/draforge
ARG TARGETPLATFORM
COPY $TARGETPLATFORM/draforge-controller ./
ENTRYPOINT ["./draforge-controller"]
