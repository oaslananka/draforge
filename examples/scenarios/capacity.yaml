apiVersion: draforge.oaslananka/v1alpha1
kind: SimulatedDevicePool
metadata:
  name: capacity-gpu-pool
  namespace: default
spec:
  driverName: sim.draforge.oaslananka
  poolName: capacity-gpu
  deviceCount: 1
  deviceType: gpu
  health: capacity-exhausted
  attributes:
    model: NVIDIA-A100-Virtual
    vendor: NVIDIA
  capacities:
    memory: 40Gi
